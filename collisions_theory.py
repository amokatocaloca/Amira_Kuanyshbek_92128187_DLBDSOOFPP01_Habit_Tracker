# #for the Player class, f.e., using a different method 

# self.hitbox = self.rect.copy().inflate((w, h))


# #the hitbox must move with the rectangle
# #in def move()

# self.hitbox.centerx = round(self.pos.x)
# self.rect.centerx = self.hitbox.centerx

